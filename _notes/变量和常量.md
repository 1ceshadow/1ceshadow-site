---
title: 变量和常量
date: 2025-06-14 15:59:09
feed: show
---
### 变量
变量 默认 不可变 😅👍
声明方式：
```rust
let x = 1;
```
如果之后
```rust
x = 6;
```
则会报错: `cannot assign twice to immutable variable`

声明成可变变量：
```rust
let mut x = 1;
x = 2;
```

### 常量
常量 不可变
常量的声明：
- 使用`const`
- **必须**注明类型
只能设置成常量表达式
```rust
const THREE_HOURS_IN_SECONDS: u32 = 60 * 60 * 3;
```
常量一般用大写和下划线命名。

### 不可变变量 和 常量 的区别

```rust
let x = 1;
```
不可变变量：
```rust
let y = x + 1; ✅
let y = 90; ✅
```
常量：
```rust
const STUDENT_NUM: u32 = 40; ✅
const STUDENT_NUM: u32 = x;  ❌
```
即：常量必须是常量表达式

### 遮蔽

使用 `let` 时，实际上创建了一个新变量
**rust**
```rust
fn main() {
    let x = 5;

    let x = x + 1;

    {
        let x = x * 2;
        println!("The value of x in the inner scope is: {x}");
    }

    println!("The value of x is: {x}");
}
```
output:
```rust
$ cargo run
   Compiling variables v0.1.0 (file:///projects/variables)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.31s
     Running `target/debug/variables`
The value of x in the inner scope is: 12
The value of x is: 6
```

**C**
```c
#include <stdio.h>

int main() {
        int x = 1;
        printf("%d\n", x);

        {
                int x = 2;
                printf("%d\n", x);
        }

        printf("%d\n", x);

        return 0;
}
```
output:
```c
$ ./a.out
1
2
1
```